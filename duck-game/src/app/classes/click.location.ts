export class ClickLocation {
  x: number;
  y: number;
  xAbsolut: number;
  yAbsolute: number;

  constructor(x: number, y: number, xAbsolute: number, yAbsolute: number) {
    this.x = x;
    this.y = y;
    this.xAbsolut = xAbsolute;
    this.yAbsolute = yAbsolute;
  }
}